# 应用标题
VITE_APP_TITLE=青羽写作平台

# 后端API地址（使用相对路径通过vite代理）
# VITE_API_BASE_URL=http://localhost:8080

# WebSocket服务器地址
VITE_WS_URL=ws://localhost:3000

# API基础URL（留空使用默认的 /api/v1，通过vite代理到后端）
# VITE_API_BASE_URL=http://localhost:8080

# CSP配置（开发环境 - 允许localhost连接）
VITE_CSP_CONNECT_SRC=ws://localhost:* http://localhost:*

# 应用端口
VITE_PORT=5173

# 是否自动打开浏览器
VITE_OPEN=true
