/**
 * Writer API 统一导出
 *
 * 使用方式：
 * import * as writerAPI from '@/modules/writer/api/writer'
 * 或
 * import { getProjects } from '@/modules/writer/api/writer'
 */

export * from './wrapper'
export { default } from './wrapper'
