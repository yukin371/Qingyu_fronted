/**
 * Finance API 统一导出
 * 提供默认导出和命名导出
 *
 * 使用方式：
 * import * as financeAPI from '@/modules/finance/api/finance'
 * 或
 * import { getWalletBalance } from '@/modules/finance/api/finance'
 */

export * from './wrapper'
export { default } from './wrapper'
